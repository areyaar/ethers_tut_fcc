const ethers = require("ethers"); // importing ethers.js
const fs = require("fs");// file system to read different files
require("dotenv").config(); // require krna pdta hai
// we can just put or .env ke contents in the console before deploying the thing, or we can encrypt the thing

async function main(){
// badme krunga
}

main()
    .then(()=> process.exit(0))
    .catch((error)=>{
        console.error(error);
        process.exit(1);
    });